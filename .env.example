# Copy to .env and fill values
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Set to 1 to call OpenAI for extraction; 0 uses the bundled deterministic
# extractor so the lab runs end-to-end without any API key.
USE_LLM=0
